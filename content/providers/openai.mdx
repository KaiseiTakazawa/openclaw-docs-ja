---
title: "OpenAI"
---

# OpenAI

# OpenAI

OpenAIは、GPTモデル用の開発者APIを提供しています。Codexは、サブスクリプションアクセスのための**ChatGPTサインイン**または使用ベースのアクセスのための**APIキー**サインインをサポートしています。CodexクラウドはChatGPTサインインを必要とします。

## オプションA: OpenAI APIキー (OpenAIプラットフォーム)

**最適:** 直接APIアクセスと使用ベースの請求。
OpenAIダッシュボードからAPIキーを取得してください。

### CLIセットアップ

```bash
openclaw onboard --auth-choice openai-api-key
# または非対話型
openclaw onboard --openai-api-key "$OPENAI_API_KEY"
```

### 設定スニペット

```json5
{
  env: { OPENAI_API_KEY: "sk-..." },
  agents: { defaults: { model: { primary: "openai/gpt-5.1-codex" } } },
}
```

## オプションB: OpenAI Code (Codex) サブスクリプション

**最適:** APIキーの代わりにChatGPT/Codexサブスクリプションアクセスを使用する。
CodexクラウドはChatGPTサインインを必要とし、Codex CLIはChatGPTまたはAPIキーサインインをサポートしています。

### CLIセットアップ (Codex OAuth)

```bash
# ウィザードでCodex OAuthを実行
openclaw onboard --auth-choice openai-codex

# またはOAuthを直接実行
openclaw models auth login --provider openai-codex
```

### 設定スニペット (Codexサブスクリプション)

```json5
{
  agents: { defaults: { model: { primary: "openai-codex/gpt-5.3-codex" } } },
}
```

### Codexトランスポートのデフォルト

OpenClawはモデルストリーミングに`pi-ai`を使用します。`openai-codex/*`モデルについては、`agents.defaults.models.<provider/model>.params.transport`を設定してトランスポートを選択できます：

* デフォルトは`"auto"`（WebSocket優先、その後SSEフォールバック）。
* `"sse"`: SSEを強制
* `"websocket"`: WebSocketを強制
* `"auto"`: WebSocketを試み、その後SSEにフォールバック

```json5
{
  agents: {
    defaults: {
      model: { primary: "openai-codex/gpt-5.3-codex" },
      models: {
        "openai-codex/gpt-5.3-codex": {
          params: {
            transport: "auto",
          },
        },
      },
    },
  },
}
```

### OpenAIレスポンスのサーバーサイド圧縮

直接OpenAIレスポンスモデル（`openai/*`で`api: "openai-responses"`を使用し、`baseUrl`が`api.openai.com`の場合）について、OpenClawは現在OpenAIサーバーサイド圧縮ペイロードヒントを自動的に有効にします：

* `store: true`を強制（モデルの互換性が`supportsStore: false`を設定しない限り）
* `context_management: [{ type: "compaction", compact_threshold: ... }]`を注入

デフォルトでは、`compact_threshold`はモデルの`contextWindow`の`70%`（または利用できない場合は`80000`）です。

### サーバーサイド圧縮を明示的に有効にする

互換性のあるレスポンスモデル（例えばAzure OpenAIレスポンス）で`context_management`の注入を強制したい場合は、これを使用します：

```json5
{
  agents: {
    defaults: {
      models: {
        "azure-openai-responses/gpt-4o": {
          params: {
            responsesServerCompaction: true,
          },
        },
      },
    },
  },
}
```

### カスタムしきい値で有効にする

```json5
{
  agents: {
    defaults: {
      models: {
        "openai/gpt-5": {
          params: {
            responsesServerCompaction: true,
            responsesCompactThreshold: 120000,
          },
        },
      },
    },
  },
}
```

### サーバーサイド圧縮を無効にする

```json5
{
  agents: {
    defaults: {
      models: {
        "openai/gpt-5": {
          params: {
            responsesServerCompaction: false,
          },
        },
      },
    },
  },
}
```

`responsesServerCompaction`は`context_management`の注入のみを制御します。直接OpenAIレスポンスモデルは、互換性が`supportsStore: false`を設定しない限り、`store: true`を強制します。

## 注意事項

* モデル参照は常に`provider/model`を使用します（詳細は[/concepts/models](/concepts/models)を参照）。
* 認証の詳細と再利用ルールは[/concepts/oauth](/concepts/oauth)にあります。