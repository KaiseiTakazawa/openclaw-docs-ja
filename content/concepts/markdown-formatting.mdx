---
title: "Markdown フォーマット"
---

# Markdown フォーマット

# Markdown formatting

OpenClaw は、チャネル固有の出力をレンダリングする前に、アウトバウンド Markdown を共有中間表現 (IR) に変換することでフォーマットします。IR は、スタイル/リンクのスパンを保持しながらソーステキストをそのまま保持するため、チャネル間でのチャンク化とレンダリングが一貫性を保つことができます。

## 目標

* **一貫性:** 一度のパースステップで複数のレンダラー。
* **安全なチャンク化:** レンダリング前にテキストを分割し、インラインフォーマットがチャンク間で壊れないようにします。
* **チャネル適合:** 同じ IR を Slack の mrkdwn、Telegram の HTML、および Signal のスタイル範囲に再パースせずにマッピングします。

## パイプライン

1. **Markdown を IR にパース**
   * IR はプレーンテキストとスタイルスパン（太字/斜体/打消し/コード/ネタバレ）およびリンクスパンから成ります。
   * オフセットは UTF-16 コードユニットで、Signal のスタイル範囲がその API と整合します。
   * テーブルは、チャネルがテーブル変換を選択した場合のみパースされます。
2. **IR をチャンク化（フォーマット優先）**
   * チャンク化はレンダリング前の IR テキストで行われます。
   * インラインフォーマットはチャンク間で分割されず、スパンはチャンクごとにスライスされます。
3. **チャネルごとにレンダリング**
   * **Slack:** mrkdwn トークン（太字/斜体/打消し/コード）、リンクは `<url|label>` として表示。
   * **Telegram:** HTML タグ（`<b>`、`<i>`、`<s>`、`<code>`、`<pre><code>`、`<a href>`）。
   * **Signal:** プレーンテキスト + `text-style` 範囲; ラベルが異なる場合、リンクは `label (url)` になります。

## IR の例

入力 Markdown:

```markdown
Hello **world** — see [docs](https://docs.openclaw.ai).
```

IR（スキーマ）:

```json
{
  "text": "Hello world — see docs.",
  "styles": [{ "start": 6, "end": 11, "style": "bold" }],
  "links": [{ "start": 19, "end": 23, "href": "https://docs.openclaw.ai" }]
}
```

## 使用される場所

* Slack、Telegram、Signal のアウトバウンドアダプターは IR からレンダリングします。
* その他のチャネル（WhatsApp、iMessage、MS Teams、Discord）は、プレーンテキストまたは独自のフォーマットルールを使用し、チャンク化前に Markdown テーブル変換が適用されます（有効な場合）。

## テーブル処理

Markdown テーブルは、チャットクライアント間で一貫してサポートされていません。`markdown.tables` を使用してチャネルごと（およびアカウントごと）に変換を制御します。

* `code`: テーブルをコードブロックとしてレンダリング（ほとんどのチャネルのデフォルト）。
* `bullets`: 各行を箇条書きポイントに変換（Signal + WhatsApp のデフォルト）。
* `off`: テーブルのパースと変換を無効にします; 生のテーブルテキストがそのまま通過します。

設定キー:

```yaml
channels:
  discord:
    markdown:
      tables: code
    accounts:
      work:
        markdown:
          tables: off
```

## チャンク化ルール

* チャンク制限はチャネルアダプター/設定から来ており、IR テキストに適用されます。
* コードフェンスは、正しくレンダリングされるようにトレーリング改行を持つ単一のブロックとして保持されます。
* リストのプレフィックスとブロック引用のプレフィックスは IR テキストの一部であるため、チャンク化はプレフィックスの途中で分割されません。
* インラインスタイル（太字/斜体/打消し/インラインコード/ネタバレ）はチャンク間で決して分割されず、レンダラーは各チャンク内でスタイルを再開します。

チャネル間のチャンク化の動作について詳しく知りたい場合は、[Streaming + chunking](/concepts/streaming) を参照してください。

## リンクポリシー

* **Slack:** `[label](url)` -> `<url|label>`; 生の URL はそのまま。ダブルリンクを避けるために、パース中にオートリンクは無効です。
* **Telegram:** `[label](url)` -> `<a href="url">label</a>`（HTML パースモード）。
* **Signal:** `[label](url)` -> `label (url)`（ラベルが URL と一致しない場合）。

## ネタバレ

ネタバレマーカー（`||spoiler||`）は Signal のみでパースされ、SPOILER スタイル範囲にマッピングされます。他のチャネルではプレーンテキストとして扱われます。

## チャネルフォーマッターの追加または更新方法

1. **一度パース:** チャネルに適したオプション（オートリンク、見出しスタイル、ブロック引用プレフィックス）を使用して共有の `markdownToIR(...)` ヘルパーを利用します。
2. **レンダリング:** `renderMarkdownWithMarkers(...)` とスタイルマーカーのマップ（または Signal スタイル範囲）を使用してレンダラーを実装します。
3. **チャンク:** レンダリング前に `chunkMarkdownIR(...)` を呼び出し、各チャンクをレンダリングします。
4. **アダプターを接続:** 新しいチャンク化ツールとレンダラーを使用するようにチャネルのアウトバウンドアダプターを更新します。
5. **テスト:** フォーマットテストを追加または更新し、チャネルがチャンク化を使用する場合はアウトバウンド配信テストを追加します。

## よくある問題

* Slack の角括弧トークン（`<@U123>`、`<#C123>`、`<https://...>`）は保持する必要があります; 生の HTML を安全にエスケープします。
* Telegram の HTML は、タグの外側のテキストをエスケープする必要があり、壊れたマークアップを避けます。
* Signal のスタイル範囲は UTF-16 オフセットに依存しているため、コードポイントオフセットを使用しないでください。
* フェンシングされたコードブロックのトレーリング改行を保持し、閉じるマーカーが独自の行に配置されるようにします。