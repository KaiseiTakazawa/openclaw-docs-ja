---
title: "コンテキスト"
---

# コンテキスト

# コンテキスト

「コンテキスト」とは、**OpenClawがモデルに送信するすべての情報**を指します。これはモデルの**コンテキストウィンドウ**（トークン制限）によって制約されます。

初心者向けのメンタルモデル：

* **システムプロンプト**（OpenClawが構築したもの）：ルール、ツール、スキルリスト、時間/実行時間、および注入されたワークスペースファイル。
* **会話履歴**：あなたのメッセージ + このセッションのアシスタントのメッセージ。
* **ツール呼び出し/結果 + 添付ファイル**：コマンド出力、ファイル読み込み、画像/音声など。

コンテキストは「メモリ」とは*異なるもの*です：メモリはディスクに保存され、後で再ロードできますが、コンテキストはモデルの現在のウィンドウ内にあるものです。

## クイックスタート（コンテキストの確認）

* `/status` → 「ウィンドウはどれくらい満杯か？」のクイックビュー + セッション設定。
* `/context list` → 注入されたもの + おおよそのサイズ（ファイルごと + 合計）。
* `/context detail` → より詳細な内訳：ファイルごと、ツールごとのスキーマサイズ、スキルエントリごとのサイズ、システムプロンプトサイズ。
* `/usage tokens` → 通常の返信に対して、返信ごとの使用フッターを追加。
* `/compact` → 古い履歴を要約してコンパクトなエントリにし、ウィンドウスペースを解放。

関連情報： [Slash commands](/tools/slash-commands), [Token use & costs](/reference/token-use), [Compaction](/concepts/compaction).

## 例の出力

値はモデル、プロバイダー、ツールポリシー、ワークスペース内の内容によって異なります。

### `/context list`

```
🧠 コンテキストの内訳
ワークスペース: <workspaceDir>
ブートストラップ最大/ファイル: 20,000 文字
サンドボックス: モード=非メイン サンドボックス=false
システムプロンプト（実行）: 38,412 文字 (~9,603 トークン) (プロジェクトコンテキスト 23,901 文字 (~5,976 トークン))

注入されたワークスペースファイル:
- AGENTS.md: OK | 生データ 1,742 文字 (~436 トークン) | 注入 1,742 文字 (~436 トークン)
- SOUL.md: OK | 生データ 912 文字 (~228 トークン) | 注入 912 文字 (~228 トークン)
- TOOLS.md: 切り捨て | 生データ 54,210 文字 (~13,553 トークン) | 注入 20,962 文字 (~5,241 トークン)
- IDENTITY.md: OK | 生データ 211 文字 (~53 トークン) | 注入 211 文字 (~53 トークン)
- USER.md: OK | 生データ 388 文字 (~97 トークン) | 注入 388 文字 (~97 トークン)
- HEARTBEAT.md: 欠落 | 生データ 0 | 注入 0
- BOOTSTRAP.md: OK | 生データ 0 文字 (~0 トークン) | 注入 0 文字 (~0 トークン)

スキルリスト（システムプロンプトテキスト）: 2,184 文字 (~546 トークン) (12 スキル)
ツール: read, edit, write, exec, process, browser, message, sessions_send, …
ツールリスト（システムプロンプトテキスト）: 1,032 文字 (~258 トークン)
ツールスキーマ（JSON）: 31,988 文字 (~7,997 トークン) (コンテキストにカウント; テキストとしては表示されない)
ツール: (上記と同じ)

セッショントークン（キャッシュ済み）: 合計 14,250 / ctx=32,000
```

### `/context detail`

```
🧠 コンテキストの内訳（詳細）
…
トップスキル（プロンプトエントリサイズ）:
- frontend-design: 412 文字 (~103 トークン)
- oracle: 401 文字 (~101 トークン)
… (+10 スキル)

トップツール（スキーマサイズ）:
- browser: 9,812 文字 (~2,453 トークン)
- exec: 6,240 文字 (~1,560 トークン)
… (+N ツール)
```

## コンテキストウィンドウにカウントされるもの

モデルが受け取るすべてのものがカウントされます。これには以下が含まれます：

* システムプロンプト（すべてのセクション）。
* 会話履歴。
* ツール呼び出し + ツール結果。
* 添付ファイル/トランスクリプト（画像/音声/ファイル）。
* コンパクション要約とプルーニングアーティファクト。
* プロバイダーの「ラッパー」または隠れたヘッダー（表示されないが、カウントされる）。

## OpenClawがシステムプロンプトを構築する方法

システムプロンプトは**OpenClaw所有**であり、各実行ごとに再構築されます。これには以下が含まれます：

* ツールリスト + 短い説明。
* スキルリスト（メタデータのみ; 下記参照）。
* ワークスペースの場所。
* 時間（UTC + 設定されている場合のユーザー時間）。
* 実行時メタデータ（ホスト/OS/モデル/思考）。
* **プロジェクトコンテキスト**の下に注入されたワークスペースブートストラップファイル。

詳細な内訳: [System Prompt](/concepts/system-prompt).

## 注入されたワークスペースファイル（プロジェクトコンテキスト）

デフォルトでは、OpenClawは固定のワークスペースファイルセットを注入します（存在する場合）：

* `AGENTS.md`
* `SOUL.md`
* `TOOLS.md`
* `IDENTITY.md`
* `USER.md`
* `HEARTBEAT.md`
* `BOOTSTRAP.md`（初回実行のみ）

大きなファイルは、`agents.defaults.bootstrapMaxChars`（デフォルトは`20000`文字）を使用してファイルごとに切り捨てられます。OpenClawはまた、ファイル全体に対して`agents.defaults.bootstrapTotalMaxChars`（デフォルトは`150000`文字）という合計ブートストラップ注入制限を強制します。`/context`は**生データと注入された**サイズを表示し、切り捨てが行われたかどうかを示します。

## スキル：注入されるものとオンデマンドで読み込まれるもの

システムプロンプトにはコンパクトな**スキルリスト**（名前 + 説明 + 場所）が含まれています。このリストには実際のオーバーヘッドがあります。

スキルの指示はデフォルトでは含まれていません。モデルは必要なときにのみスキルの`SKILL.md`を`read`することが期待されています。

## ツール：2つのコストがある

ツールはコンテキストに2つの方法で影響を与えます：

1. **システムプロンプト内のツールリストテキスト**（あなたが「Tooling」として見るもの）。
2. **ツールスキーマ**（JSON）。これらはモデルに送信され、ツールを呼び出すことができます。これらはプレーンテキストとしては表示されませんが、コンテキストにカウントされます。

`/context detail`は最大のツールスキーマを内訳し、何が支配しているかを確認できます。

## コマンド、ディレクティブ、および「インラインショートカット」

スラッシュコマンドはGatewayによって処理されます。いくつかの異なる動作があります：

* **スタンドアロンコマンド**：メッセージが単に`/...`の場合、コマンドとして実行されます。
* **ディレクティブ**：`/think`, `/verbose`, `/reasoning`, `/elevated`, `/model`, `/queue`は、モデルがメッセージを見る前に削除されます。
  * ディレクティブのみのメッセージはセッション設定を保持します。
  * 通常のメッセージ内のインラインディレクティブは、メッセージごとのヒントとして機能します。
* **インラインショートカット**（許可された送信者のみ）：通常のメッセージ内の特定の`/...`トークンは即座に実行されることができます（例：「hey /status」）、そして残りのテキストがモデルに見られる前に削除されます。

詳細： [Slash commands](/tools/slash-commands).

## セッション、コンパクション、およびプルーニング（持続するもの）

メッセージ間で持続するものはメカニズムによって異なります：

* **通常の履歴**は、ポリシーによってコンパクト化/プルーニングされるまでセッショントランスクリプトに持続します。
* **コンパクション**は要約をトランスクリプトに持続し、最近のメッセージをそのまま保持します。
* **プルーニング**は、実行のための*メモリ内*プロンプトから古いツール結果を削除しますが、トランスクリプトを書き換えることはありません。

ドキュメント： [Session](/concepts/session), [Compaction](/concepts/compaction), [Session pruning](/concepts/session-pruning).

## `/context`が実際に報告するもの

`/context`は、利用可能な場合、最新の**実行構築**システムプロンプトレポートを優先します：

* `System prompt (run)` = 最後の埋め込まれた（ツール対応）実行からキャプチャされ、セッションストアに保持されます。
* `System prompt (estimate)` = 実行レポートが存在しない場合（またはレポートを生成しないCLIバックエンド経由で実行している場合）にその場で計算されます。

いずれにせよ、サイズと主要な寄与者を報告します。完全なシステムプロンプトやツールスキーマをダンプすることはありません。