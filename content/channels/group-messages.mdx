---
title: "グループメッセージ"
---

# グループメッセージ
# Group messages (WhatsApp web channel)
目的: ClawdがWhatsAppグループに参加し、ピンを受けたときだけ起動し、そのスレッドを個人のDMセッションとは別に保つことです。
注意: `agents.list[].groupChat.mentionPatterns`は現在、Telegram/Discord/Slack/iMessageでも使用されています。このドキュメントはWhatsApp特有の動作に焦点を当てています。マルチエージェントのセットアップの場合、エージェントごとに`agents.list[].groupChat.mentionPatterns`を設定するか、グローバルフォールバックとして`messages.groupChat.mentionPatterns`を使用してください。
## 実装内容 (2025-12-03)
\* アクティベーションモード: `mention`（デフォルト）または`always`。`mention`はピンを必要とします（実際のWhatsAppの@メンション、`mentionedJids`、正規表現パターン、またはテキスト内のボットのE.164）。`always`はすべてのメッセージでエージェントを起動しますが、意味のある価値を追加できる場合のみ返信する必要があります。そうでない場合は、サイレントトークン`NO\_REPLY`を返します。デフォルトは設定ファイル（`channels.whatsapp.groups`）で設定でき、グループごとに`/activation`を介して上書きできます。`channels.whatsapp.groups`が設定されている場合、それはグループの許可リストとしても機能します（すべてを許可するには`"\*"`を含めます）。
\* グループポリシー: `channels.whatsapp.groupPolicy`は、グループメッセージが受け入れられるかどうかを制御します（`open|disabled|allowlist`）。`allowlist`は`channels.whatsapp.groupAllowFrom`を使用します（フォールバック: 明示的な`channels.whatsapp.allowFrom`）。デフォルトは`allowlist`です（送信者を追加するまでブロックされます）。
\* グループごとのセッション: セッションキーは`agent::whatsapp:group:`のようになり、`/verbose on`や`/think high`などのコマンド（スタンドアロンメッセージとして送信）もそのグループにスコープされます。個人のDM状態は影響を受けません。ハートビートはグループスレッドではスキップされます。
\* コンテキスト注入: \*\*保留中のみ\*\*のグループメッセージ（デフォルト50）で、実行をトリガーしなかったものは、`[Chat messages since your last reply - for context]`の下にプレフィックスが付けられ、トリガーとなった行は`[Current message - respond to this]`の下に表示されます。セッション内に既にあるメッセージは再注入されません。
\* 送信者の表示: すべてのグループバッチは、最後に`[from: Sender Name (+E164)]`で終わるため、Piは誰が話しているかを知ることができます。
\* エフェメラル/一度だけ表示: テキスト/メンションを抽出する前にそれらを解凍するため、それらの内部のピンもトリガーされます。
\* グループシステムプロンプト: グループセッションの最初のターン（および`/activation`がモードを変更するたびに）に、システムプロンプトに短い説明文を注入します。例えば、`You are replying inside the WhatsApp group "". Group members: Alice (+44...), Bob (+43...), … Activation: trigger-only … Address the specific sender noted in the message context.` メタデータが利用できない場合でも、エージェントにはそれがグループチャットであることを伝えます。
## 設定例 (WhatsApp)
`~/.openclaw/openclaw.json`に`groupChat`ブロックを追加して、WhatsAppがテキスト本文内の視覚的な`@`を削除しても表示名のピンが機能するようにします:
```json5
{
channels: {
whatsapp: {
groups: {
"\*": { requireMention: true },
},
},
},
agents: {
list: [
{
id: "main",
groupChat: {
historyLimit: 50,
mentionPatterns: ["@?openclaw", "\\+?15555550123"],
},
},
],
},
}
```
注意:
\* 正規表現は大文字と小文字を区別しません。`@openclaw`のような表示名のピンと、`+`やスペースの有無にかかわらず生の番号をカバーします。
\* 誰かが連絡先をタップしたとき、WhatsAppは依然として`mentionedJids`を介して標準的なメンションを送信するため、番号のフォールバックはほとんど必要ありませんが、有用なセーフティネットです。
### アクティベーションコマンド（オーナー専用）
グループチャットコマンドを使用します:
\* `/activation mention`
\* `/activation always`
この変更を行えるのはオーナー番号のみ（`channels.whatsapp.allowFrom`から、または設定されていない場合はボットのE.164）です。グループ内でスタンドアロンメッセージとして`/status`を送信して、現在のアクティベーションモードを確認します。
## 使用方法
1. あなたのWhatsAppアカウント（OpenClawを実行しているもの）をグループに追加します。
2. `@openclaw …`（または番号を含める）と言います。許可リストにある送信者のみがトリガーできますが、`groupPolicy: "open"`を設定すれば可能です。
3. エージェントプロンプトには最近のグループコンテキストと、末尾の`[from: …]`マーカーが含まれるため、正しい人に対処できます。
4. セッションレベルの指令（`/verbose on`、`/think high`、`/new`または`/reset`、`/compact`）は、そのグループのセッションにのみ適用されます。スタンドアロンメッセージとして送信して登録させます。あなたの個人DMセッションは独立したままです。
## テスト / 検証
\* 手動スモーク:
\* グループ内で`@openclaw`ピンを送信し、送信者名を参照する返信を確認します。
\* 2回目のピンを送信し、履歴ブロックが含まれ、次のターンでクリアされることを確認します。
\* ゲートウェイログをチェックします（`--verbose`で実行）し、`inbound web message`エントリが`from: `と`[from: …]`サフィックスを表示していることを確認します。
## 既知の考慮事項
\* ハートビートは、騒がしいブロードキャストを避けるために意図的にグループではスキップされます。
\* エコ抑制は、結合されたバッチ文字列を使用します。メンションなしで同じテキストを2回送信した場合、最初のものだけが応答を受け取ります。
\* セッションストアのエントリは、セッションストア内で`agent::whatsapp:group:`として表示されます（デフォルトで`~/.openclaw/agents//sessions/sessions.json`）。エントリが欠けている場合は、グループがまだ実行をトリガーしていないことを意味します。
\* グループ内のタイピングインジケーターは`agents.defaults.typingMode`に従います（デフォルト: メンションされていない場合は`message`）。