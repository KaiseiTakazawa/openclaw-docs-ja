---
title: "reset"
---

# reset

# `openclaw reset`

ローカルの設定/状態をリセットします（インストールされたCLIは保持されます）。

```bash
openclaw reset
openclaw reset --dry-run
openclaw reset --scope config+creds+sessions --yes --non-interactive
```