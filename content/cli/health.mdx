---
title: "health"
---

# health

# `openclaw health`

実行中の Gateway からヘルスを取得します。

```bash
openclaw health
openclaw health --json
openclaw health --verbose
```

注意事項:

* `--verbose` はライブプローブを実行し、複数のアカウントが設定されている場合にアカウントごとのタイミングを出力します。
* 出力には、複数のエージェントが設定されている場合のエージェントごとのセッションストアが含まれます。